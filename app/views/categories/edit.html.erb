<h3 class="text-center mt-4">Edit category name</h3>
<div class="container">

    <div class="row d-flex justify-content-center">
        <div class="col-10">
            <%= render 'form' %>
        </div>

        <div class="mb-3">
            <p><%= link_to '[ Cancel and Return to category ]', category_path(@category), class: "d-flex justify-content-center mt-4 text-success" %></p>
        </div>

    </div>

</div>